export function setToken(state, token) {
  state.token = token;
}

export function setTokenExpiredAt(state, exp) {
  state.tokenExpiredAt = exp;
}

export function setUser(state, user) {
  state.user = user;
}
